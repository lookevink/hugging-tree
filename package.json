{
  "name": "hugging-tree",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "generate:openapi": "python scripts/generate-openapi.py",
    "generate:sdk": "python scripts/generate-sdk.py",
    "generate:all": "python scripts/generate-openapi.py && python scripts/generate-sdk.py",
    "dev": "cd hugging-tree-fe && pnpm run dev",
    "build": "cd hugging-tree-fe && pnpm run build",
    "lint": "cd hugging-tree-fe && pnpm run lint"
  }
}

